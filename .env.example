# ---- Database: use ONE of the following ----

# Option A: PostgreSQL (leave empty if using Firebase)
DATABASE_URL=postgresql://user:password@localhost:5432/bilyar

# Option B: Firebase / Firestore (set project id to use Firestore instead of PostgreSQL)
# FIREBASE_PROJECT_ID=bilyar
# Then set ONE of:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"bilyar",...}

# ---- Payment ----

# MyFatoorah (Card / KNET) - leave empty for demo mode
# Test: MYFATOORAH_BASE_URL=https://apitest.myfatoorah.com and use a test token (see docs/MYFATOORAH_TESTING.md)
# Optional: uncomment below to use MyFatoorah public test token (testing only)
# MYFATOORAH_API_KEY=SK_KWT_vVZlnnAqu8jRByOWaRPNId4ShzEDNt256dvnjebuyzo52dXjAfRx2ixW5umjWSUx
MYFATOORAH_API_KEY=
MYFATOORAH_BASE_URL=https://apitest.myfatoorah.com

# Deema BNPL (Buy Now Pay Later) - leave empty for demo mode
# Sandbox: staging-api.deema.me. Live: api.deema.me (after go-live)
# Use Sandbox secret API_KEY from Deema Merchant Portal.
DEEMA_API_KEY=
DEEMA_BASE_URL=https://staging-api.deema.me
# DEEMA_WIDGET_KEY=   # Optional: Sandbox WIDGET_KEY if you add the Deema widget later
# Webhook: set the same secret as in Deema Merchant Portal (Headers key/value)
DEEMA_WEBHOOK_SECRET=bilyar-deema-webhook-2026
# DEEMA_WEBHOOK_HEADER=x-webhook-secret

# Optional: server port (default 5000)
# PORT=5000

# ---- WhatsApp Cloud API (Meta) ----
# Get from developers.facebook.com → WhatsApp → API Setup
# WHATSAPP_ACCESS_TOKEN=EAAxxxxx
# WHATSAPP_PHONE_NUMBER_ID=123456789
# Public site URL (needed for invoice PDF link in order received message)
# SITE_URL=https://your-app.onrender.com
# Optional: custom template names (defaults: order_received, order_shipped, marketing_message)
# WHATSAPP_TEMPLATE_ORDER_RECEIVED=order_received
# WHATSAPP_TEMPLATE_ORDER_SHIPPED=order_shipped
# WHATSAPP_TEMPLATE_MARKETING=marketing_message
