rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Backend (Admin SDK) has full access. These rules apply to client access if you add Firebase client SDK later.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
